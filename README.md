# simple_process_script
script to run simple process in linux
# Module Function
1. Change_header.sh: Change the header of the file to the desired header 
2. docker_load.sh: Automatically docker load all tar file in the directory specified
